workflow:
  steps:
    - set_flags:
      flags:
        - type: build
          status: disable
          project: home:Admin
          architecture: i586
          repository: openSUSE_Tumbleweed
  filters:
    event: pull_request
