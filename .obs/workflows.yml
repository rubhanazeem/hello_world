version: '1.1'
testbuild:
  steps:
    - branch_package:
        source_project: home:Admin
        source_package: hello_world
        target_project: home:Iggy
  filters:
    event: pull_request
    labels:
      only:
        - duplicate
